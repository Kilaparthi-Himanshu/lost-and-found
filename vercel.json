{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "installCommand": "npm install --force && npm install --force postcss autoprefixer tailwindcss@latest"
        }
      }
    ]
  }